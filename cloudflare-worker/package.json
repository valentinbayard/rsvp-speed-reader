{
  "name": "rsvp-reader-proxy",
  "version": "1.0.0",
  "description": "Cloudflare Worker proxy for RSVP Speed Reader article extraction",
  "main": "worker.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "dependencies": {
    "@penseapp/readability": "^0.5.0",
    "linkedom": "^0.16.0"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  },
  "keywords": [
    "cloudflare",
    "worker",
    "rsvp",
    "readability"
  ]
}
